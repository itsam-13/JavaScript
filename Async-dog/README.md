# Async Dog Fetcher

Small demo showing `async/await` and parallel requests via `Promise.all`.

Usage
- Open [Async-dog/index.html](Async-dog/index.html) in a browser.
- Click **Fetch Random Dog** to fetch a single image.
- Click **Fetch 3 Dogs (Parallel)** to fetch three images concurrently.
